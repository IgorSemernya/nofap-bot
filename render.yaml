services:
  - type: web
    name: nofap-bot
    runtime: python
    envVars:
      - key: BOT_TOKEN
        sync: false
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    env:
      PYTHON_VERSION: "3.11.9"  # ← фиксируем стабильную версию